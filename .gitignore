# Keep all project files in the repository except local ZIP backup archives
# The user requirement is to upload everything except the ZIP archive files.
*.zip
*.7z
*.rar

# Keep model artifacts tracked if desired
# Uncomment if you do NOT want to upload model files
# artifacts/
