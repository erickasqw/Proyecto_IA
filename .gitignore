# Only exclude the specific backup ZIP files that should not be uploaded
archive (7).zip
archive (8).zip
archive (9).zip

# Keep all other project files tracked normally
# This allows the project code, datasets, PDFs, and local assets to be included.
